{% extends "layout.html.twig" %}

{% block title %}{{ parent() }} - {{ post.title }}{% endblock %}

{% block content %}
<article>
    <h2>{{ post.title }}</h2>

    {% include '_post_info.html.twig' %}

    {{ post.content | raw }}
</article>
{% endblock %}
